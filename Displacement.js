var Displacement = function(sandbox) {
  this.sandbox = sandbox;
};

Displacement.prototype.moveSand = function(x, y, prevX, prevY) {
  var changedGrains = new CoordSet();
  //TODO move sand
  return changedGrains;
};
